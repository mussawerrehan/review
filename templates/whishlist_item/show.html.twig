{% extends 'base.html.twig' %}

{% block title %}WhishlistItem{% endblock %}

{% block body %}
    <h1>WhishlistItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ whishlist_item.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('whishlist_item_index') }}">back to list</a>

    <a href="{{ path('whishlist_item_edit', {'id': whishlist_item.id}) }}">edit</a>

    {{ include('whishlist_item/_delete_form.html.twig') }}
{% endblock %}
